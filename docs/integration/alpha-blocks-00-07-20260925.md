# Alpha blocks 00–07 backfill — integration record

Status: LOCAL VALIDATION PASSED; alpha integration pending. Audit process paused by owner. No release acceptance,
physical-device acceptance, Copilot request, or artifact publication is implied.

## Scope

Owner requested alpha source, tests, and this integration record only. Preserve
alpha's later features. The working branch is
`backfill/alpha-blocks-00-07-20260925`; the source alpha baseline is
`0c523dad0cb7532dfb6c2095efa4aca9588121e4`.

This is a corrective replay onto alpha, not a merge of the extracted release
slices. The slices omit features that alpha already implements.

## Pinned correction sources

| Unit | Original extracted candidate | Correction head |
| --- | --- | --- |
| 00a | `a9173b32eb8515db8130f0bd393bc9b249d83320` | `9cb72384fd14b2a688a70d17711a7923941d2290` |
| 00b | `fa971c617e16612be0af67ca91a0838b98f38544` | `78ba412963c05471da73799592963f3ca7e760dd` |
| 01 | `66ca726662822a9ff102bcd1354cc6a39d5a203b` | `752466b2519fadc88881e52c64e604948fe0b615` |
| 02 | `51411c4a7c96137bc80f8b99fa1690c4e385f7fd` | `9237d0a30004dbc079dcf3c1577b2895bc50c73a` |
| 03 | `210e5e155e3ae10b7975a6480e371c48cfc9525a` | `5aa047e4d07d8f3a59b25c6cd80a48025aa7e418` |
| 04 | `9e6c56c267c6755f55364f2a48073880b4f6f6e4` | `f05bfe96dadc5a2deaf47437a5d3f5e40d6c352f` |
| 05 | `fd033f6ef815e7e49964cbdd53212fb35ea46e37` | `5c3f2d927f56a8646178d00c0abc9ed01c427393` |
| 06 | `2ec1e4d6d45f430462e10a9fb4ba09203736fe1c` | `9a177aa21253eae866bb408d168c4ea1c1e1c9cb` |
| 07 | `632de4f7f9fd864d9bb1555ea5a7179131a203b8` | `a94c3507b0321c5be0af1f9b17d0f18a1e0716e1` |

A later fetch observed 00b `fbcc766725a41c9c873332b36b4b026d0f4ef92a`:
its Ripple remaining-capacity correction/test and EIP-712 error-table assertion
are included. Its release-slice protobuf option removals do not apply to alpha's
active Solana/Osmosis features. Alpha already separates USER_CANCELLED from the
error table.

A later fetch observed 07 `8c654bcd8e1c8936a86aaa625a4ab78900a250e9`:
it narrows release ERC-7730 to raw fields/index paths and adds DebugLink text
instrumentation. It is not the pinned replay candidate. Applying its raw-only
capability table/host mirror to alpha would disable existing formatter, array,
condition, and embedded-call features. This work does not claim acceptance of
that ongoing, separately owned feature work.

## Reconciliation decisions

- Retain alpha V20 framed storage, passkeys, KDF v2, and explicit burned-format
  handling for versions 18/19. Add newer normal-band storage refusal without
  overwriting flash; preserve the Bitcoin-only wallet lock.
- Retain alpha recovery's complete coded-character history across arbitrary
  backspaces. Port private display-scratch clearing and dice/BIP85 privacy
  probes; retain the 23-word-boundary regression test.
- Retain alpha Zcash privacy/signing APIs, contact address review, Solana LUT,
  certified metadata, full ERC-7730 formatters and embedded calls.
- Alpha has no Binance engine. Cross-workflow tests use Osmosis/Cosmos; omitted
  Binance-only cases do not represent alpha functionality.
- Preserve root-certified catalog verification. Port the release runtime-key
  path only with AdvancedMode and an explicitly loaded signer; identify that
  path as Runtime signer / NOT verified by KeepKey in the device review.
  Certified signer identity remains distinct. Verify both trust paths.
- Retain alpha's calldata replay binding and full-document EIP-712 replay
  binding. Add domain constraints, escaped field disclosures, source/intent
  disclosure, explicit final typed-data consent, and response construction
  after final approval.
- Canonical decimal encoding supports 256-bit integers. Update old int64-only
  overflow tests to actual int256 limits; noncanonical whitespace/sign/negative
  zero forms must fail without hash mutation. Keep alpha chain IDs above 2^32.
- Retain full/BTC build identities and alpha CI conventions. Alpha has no staged
  capability exemptions; absence of a capability ledger grants no waivers.
- Crypto advances from alpha/07 `8a392f70a5d5575ece3dfb35f115d4a4b27f497c`
  to 00b `74908938c562ec89dcae9fb4e3b05ec3ff17f58b`, its descendant. The later
  slice had lost 00a/00b Base32/Base58 bounded buffers and negative-length guards;
  the imported regression exposed this as a crypto-unit crash. Protocol `dd9c85dc747cf965fb0e7bf49615dc9e7568ee65`
  and host base `9cea0adfab4a5d99a81a42ee09f5caba9616edbb` descend from alpha's pins.
  Host integration adds the missing 00b recovery handshake/subpage tests and
  02 dice-privacy driver (`11ea9f4`, `98c717ff`, `ea385cf6`), followed by alpha
  review expectations. Final host pin is recorded in the JSON manifest; its
  nested protocol is synchronized.
- Retain alpha's explicit external-recipient Uniswap review: the host test
  checks the complete recipient disclosure and historical signature, and
  requires cancellation to refuse signing. Retain Solana's fixed token
  precision; assert the expanded 64-bit fingerprint using the certificate's
  public key. Unknown-token OLED hashes were checked against captured screens.
- Keep shared-library linking in `lib/emulator` only; the replay initially left
  a duplicate cross-directory link block incompatible with pinned CMake 3.7.
  All four lifecycle regressions pass in the isolated Linux shared-library build.
- Include the native ERC-7730 validator with the exact emulator build in the
  host-test image. Full-product host tests fail if that validator is absent.

## Canonical dependency handoff

The host corrections are published on fork master. Protocol master was merged
with the tested LUT head, retaining master's package version 7.19.0; only package
metadata differs from the tested protocol tree. Both firmware and host now pin
that reconciled master. Crypto corrections are published on the fork's canonical
`keepkey` branch (this repository has no master); `.gitmodules` now names that
fork. All three pins are recorded in the manifest and reachable from canonical
fork branches. No upstream branch was modified.

## Evidence preservation and ownership

The dirty canonical firmware worktree and its unresolved files were preserved.
Its untracked release documents and the paused Stack 08 worktree were archived
before this integration under `/private/tmp/kk-alpha-0007-evidence`, with hashes
in `preservation.json`. That directory also holds the source manifest, per-unit
path inventories, patches, and build logs. The integration uses its own worktree.
Agent 2 retains Stack 07; agent 3 retains 00b. No audit claim is transferred.

## Local validation and integration gate

| Check | Result |
| --- | --- |
| Full firmware native | 947 passed |
| Bitcoin-only firmware native | 212 passed |
| Board / crypto / Pallas / Zcash support targets | All selected targets passed in applicable variants |
| Shared-library lifecycle | 4 passed |
| Targeted full host contracts | 50 passed, 3 skipped |
| Targeted Bitcoin-only host contracts | 18 passed, 35 skipped (absent altcoin/EVM features) |
| Broad alpha host compatibility | 802 passed, 34 skipped |
| Solana LUT after capability advertisement | 4 passed (these had been skipped in the broad run) |
| Report gate regressions | 7 passed |
| Full/BTC ARM | Both linked; unchanged 16 KiB minimum runtime SRAM gate |
| Formatting / patch whitespace | Pinned clang-format 20 scope and git diff checks pass |

The first merged ARM build missed the runtime reserve by 568 bytes. Entropy now
reuses decoded request scratch after copying size and completing consent,
retaining alpha's 8,192-byte response API and recovering 5,984 bytes. Full reserve
is 21,800 bytes. The ERC-7730 host state ceiling is 4,368 bytes, accounting for
domain constraints, signer identity, and replay digest; the device linker remains
the resource gate.

The protocol pin added a Solana LUT capability field. Alpha advertises its
implemented feature explicitly; Bitcoin-only advertises false. The four existing
positive/refusal host controls now run and pass. Source/intent-only typed review,
full-domain replay binding, calldata mutation refusal, malformed integer/bytes
validation, and consent refusal were checked during reconciliation.

The JSON validation record lists evidence hashes and limitations. Explicit
withheld controls include the official ERC-7730 registry dataset (not configured),
UDP-inapplicable host dylib cases, and two process-discovery-dependent power-cycle
cases under Docker emulation. Physical-device validation and release/audit
acceptance remain separate. No Copilot review was requested.

GitHub PR validation and alpha integration remain pending. Alpha's push workflow
publishes rolling emulator libraries; a source-only merge must suppress that push
run after PR checks, preserving the owner's source/tests/integration-record scope.

Docker cleanup was owner-authorized after API failures: 13.2 GB unused build
cache and 11 obsolete agent-1 audit images removed. Active containers, volumes,
current validation images, and pinned builder were preserved; no restart performed.
