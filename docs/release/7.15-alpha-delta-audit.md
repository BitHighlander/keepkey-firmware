# 7.15 alpha delta audit

This ledger compares the fixed alpha source `78c54c316` with the staged 7.15
release. It records policy decisions; commit equivalence is not expected where
the release implementation was adapted to preserve its audit boundaries.

| Alpha change | 7.15 disposition | Evidence / boundary |
| --- | --- | --- |
| #783–#802 ERC-7730 display VM and formatter series | Included, adapted | Firmware stacks 7–8 (#837–#838), including the #820 CI/correctness follow-ups and #823 calldata binding. All entry points require AdvancedMode. |
| #817 Solana plain-text message signing | Included, adapted | Stack 00b (#845). Only bounded printable text that does not embed the signer key bypasses the opaque-message AdvancedMode gate. |
| #829 Solana schema v2 | Included, adapted | Stack 00b (#845). Runtime signer only; the ordinary transaction review remains and opaque signing still requires AdvancedMode. |
| #824 python-keepkey alpha pin | Excluded | The alpha pin includes unrelated and policy-incompatible work. Release companions are pinned independently to minimal reviewable commits. |
| #830 built-in KeepKey clearsign root | Excluded | 7.15 has no compiled verification root, certified/warning-free identity path, or persistent signer. Runtime signers are session-only and AdvancedMode-only. |

The release advances only along the canonical python-keepkey
`reconcile/upstream-sync` ancestry. The locally validated final pin is
`3e1689d3ec6ffb68c8aefbbb091e0fca2b78cd4b`; its parent
`49d537ce953b7524599bfc87f56b7a50a51a13a1` is the exact Docker-tested
runtime/test tree, and the head changes only the hosted workflow's immutable
7.15 firmware refs. Intermediate pins remain on the same ancestry for their
EOS and Solana capability gates. The same companion line supports older
firmware through feature/version gating rather than release-specific branches.

Final local evidence at firmware code head
`9b0583a41b758af7fb4c08280ab2a2bd8bb5d503`: clang-format 20, cppcheck,
actionlint, both emulator product builds, and both native suites pass. The
regular canonical Docker profile passes 755 tests with 75 deliberate skips;
all five owned power-cycle storage controls now execute instead of skipping.
These results establish local convergence, not
permission to merge or release; corrected PR publication/checks and human
source/hardware audit remain required.

## Executable policy lock

- Clearing runtime signers leaves every metadata key slot unresolved.
- Metadata processing and generic/runtime attestation verification are inert
  when AdvancedMode is disabled.
- Runtime identities receive an explicit identity presentation; the display
  code fails closed if metadata ever arrives from a non-runtime signer source.
- ERC-7730 and Solana schema entry points independently require AdvancedMode,
  so a future parser or transport change cannot bypass the signer policy.

## Deferred alpha-only trust features

Root certificates, delegated certificates, certified Solana lookup-table
proofs, certified token definitions, and Portals/dynamic certified decoders are
not part of 7.15. They require a separately authorized trust-policy release.

The release therefore carries the alpha decoding and display capability while
deliberately excluding alpha's firmware-owned trust root and every feature that
depends on it.
