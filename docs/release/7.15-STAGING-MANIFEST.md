# Unmerged 7.15 staging stack

Owner direction: 2026-09-08. Internal staging is agent-controlled. Nothing in
this stack merges into fork develop; human release review is not a prerequisite
to authoring the stack. The objective is the 7.14.2 foundation plus a coherent,
tested extraction of alpha 7.15 functionality, ready for upstream review.

## Frozen identities

- Fork develop target preserved: `da075b8cb717b56dc1023edb52c2ccdf171b8a08`.
- Audited 7.14.2 source: `e519dc13ced6adbd77cc5ceb6207f3cc04e57492`.
- Foundation including H01–H03: `ad16caf5fe2b0e14f2bf9ee1fb0df64ff95a2c57`.
- Reconstruction commit: `bb76338fd`; its complete tree equals that foundation.
- Alpha extraction source: `91af1cfc8ee3936ae3df30e227fe1bf8fce4168b`.
- Historical 7.15 split anchor: `5ea56d59f` (source guide, not a merge target).
- Dirty alpha worktree edits are not an immutable source and are not imported.

The foundation replacement intentionally omits the later features currently on
fork develop, including delegated/reductive clear-signing, passkeys, Orchard,
Hive, Taproot, dice and BIP-85. They remain preserved at the target/source SHAs.
Omission from the foundation is not rejection from the release roadmap: selected
7.15 features return only through explicit feature units below. No broad alpha
merge is part of this reconstruction.

## Selected dependency order

| Unit | Behavior | Depends on | Acceptance |
| --- | --- | --- | --- |
| F00 | Exact 7.14.2 + H01–H03 foundation; corrected stacking SOP | Existing fork develop as PR target only | Tree identity before documentation overlay; final PR CI and review |
| F01 | SRAM reserve/frame budget for growing 7.15 image | F00 | ARM symbols/frame evidence; enforce reserve without weakening limits |
| F02 | Session-only AdvancedMode | F01 | No flash persistence; teardown resets; unchanged other policies; regression tests |
| F03 | Runtime provider identities and signed metadata verification | F02; explicit protocol/display prerequisites where needed | Opt-in, RAM-only trust; signature/binding tests; no pinned keys |
| F04 | Additive EVM provider context | F03 | Original raw review preserved; failed metadata falls back; cancellation aborts |
| F05 | Additive Solana transaction-bound account context | F03; explicit Solana prerequisites | Transaction/account binding; fallback; raw review preserved |

Token-table budgeting is part of the resource plan. Its policy lives in the
Python dependency: inspect that exact range before selecting a dependency
update; do not substitute a broad future-feature test pin just to obtain it.

## Evidence and reconstruction

Each feature branch starts at its predecessor head and its PR targets that
branch. Record exact final heads, Copilot round counts and CI receipts as units
finish. Stop requesting reviews for accepted heads; unrelated findings receive
separate dispositions. Revalidate affected evidence if a predecessor changes.

F00–F05 are not certified complete by this planning manifest. Full assembled
stack validation and a final reconstruction/pin check remain required. Physical
OLED/release acceptance stays explicitly separate from internal PR staging.

F01 resource remediation: Ripple base58 now uses fixed arrays bounded by the
existing 128-byte check payload limit (132 with checksum). Both private helpers
reject larger binary buffers, and signed negative lengths are rejected before
allocation/copy. All four local Ripple tests pass, including every payload
length 1–128 and invalid boundaries. This removes four variable-length arrays;
ARM frame confirmation remains pending. The available local ARM compiler lacks
its C library headers, so no local ARM success is claimed. The five dynamic
frames in the shared crypto dependency remain unresolved; no gate waiver.
