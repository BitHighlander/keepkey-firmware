# Firmware 7.15 audit handoff

Date: 2026-09-21

Status: **locally converged; firmware publication, CI, and human audit pending**. The
exact final tree below passes the pinned formatter and static-analysis gates,
both emulator product builds, both native unit-test profiles, both canonical
integration profiles, and both strict OLED evidence gates. Earlier ARM evidence
is retained below as regression history, but must be regenerated on the
published final SHA before release.
Explicit human authorization is required before hardware/release testing or
merge.

This document is the entry point for a new auditor. It describes the stack,
the intended security boundary, the highest-risk code surfaces, the companion
repositories, current evidence, known failures, and a reproducible review
order. It supplements rather than replaces the focused documents under
`docs/release/audit-units/`, `docs/release/7.15-alpha-delta-audit.md`, and
`docs/security/HANDOFF-ERC7730-ALPHA.md`.

## Non-negotiable release policy

- Metadata signers are loaded at runtime only.
- Runtime signers are session-scoped and require AdvancedMode.
- Disabling AdvancedMode, initializing a new session, authorization loss, or
  workflow teardown must make prior metadata and signer identity unusable.
- No compiled-in KeepKey or alpha trusted root is permitted.
- Runtime metadata is additive: it must not suppress the ordinary/raw review.
- Certified roots, delegate certificates, certified Solana lookup-table
  proofs, certified token definitions, and Portals/dynamic certified decoders
  are excluded from 7.15.
- Regular and bitcoin-only are the only release products.
- python-keepkey remains one canonical multi-release branch. Release-specific
  companion forks are not an acceptable gate mechanism.
- Copilot review has not been requested. It requires explicit human approval.

## Exact locally validated heads

- firmware code/workflow tree before this handoff-only update: `9b0583a41b758af7fb4c08280ab2a2bd8bb5d503`
- firmware code/workflow tree object: `1c8e42dadef70ccbebb04a2b5c4630b2dd2cfe23`
- python-keepkey: `49d537ce953b7524599bfc87f56b7a50a51a13a1`
- device-protocol: `dd9c85dc747cf965fb0e7bf49615dc9e7568ee65`
- trezor-firmware/crypto: `8a392f70a5d5575ece3dfb35f115d4a4b27f497c`
- regular emulator Docker image: `sha256:7be45a8cc3cb4a8054d746ed7a5590a6378616a9a26b5755b8d837176cfb6d3d`
- regular python-keepkey test image: `sha256:a6c15c8a6d4174aac854d1b1a3278f7de3bac929c0f4d3498f8f3f258cb8292a`
- bitcoin-only emulator Docker image: `sha256:6f01bc8fa42a4c9a621ad223d932b70a56409c16d369b2ae366c372f8483b2b0`
- bitcoin-only python-keepkey test image: `sha256:a9833fa9d90731623eec62712693568044d1b9be6ab43ba6752183eb0a7f6b73`

The worktrees were clean when these identifiers and results were recorded.
Docker ran the pinned amd64 release environment on an arm64 host; the platform
warning is expected and is not a skipped build or test.

## Stack topology and review units

Review adjacent PR diffs in numeric order. Do not review each head against
`develop`; that repeats all earlier slices and obscures ownership.

Two earlier topology calculations were invalid. The first had a 23,381-line
stack 01 and an empty stack 12. The next used a stale local `develop` ref and
reported stack 00 as 19,773 lines when GitHub correctly calculated 33,739.
The release foundation is therefore split into 00a and 00b below. These cuts do
not change the final firmware tree.

| Stack | Validated code head | Adjacent base | Code lines | Primary audit surface |
| --- | --- | --- | ---: | --- |
| 00a / [#844](https://github.com/BitHighlander/keepkey-firmware/pull/844) | `a9173b32e` | `develop` | 19,275 | audited 7.14.x carryforward, release/build foundations and product workflow repair |
| 00b / [#845](https://github.com/BitHighlander/keepkey-firmware/pull/845) | `4c56e19e3` | stack 00a | 18,416 | remaining release foundation, storage/setup, Solana release policy, scope repair and executable power-cycle gates |
| 01 / [#831](https://github.com/BitHighlander/keepkey-firmware/pull/831) | `66ca72666` | stack 00b | 3,680 | remaining EVM/release foundations and canonical companion pins |
| 02 / [#832](https://github.com/BitHighlander/keepkey-firmware/pull/832) | `51411c4a7` | stack 01 | 3,891 | THORChain, XRP, Hive and Zcash build/signing contracts |
| 03 / [#833](https://github.com/BitHighlander/keepkey-firmware/pull/833) | `2e008f852` | stack 02 | 575 | recovery, BIP-85 and boot verification |
| 04 / [#834](https://github.com/BitHighlander/keepkey-firmware/pull/834) | `9e6c56c26` | stack 03 | 47 | Solana, TRON, THOR/Maya and TON chain signing follow-ups |
| 05 / [#835](https://github.com/BitHighlander/keepkey-firmware/pull/835) | `dc6446003` | stack 04 | 125 | storage/product variant boundary |
| 06 / [#836](https://github.com/BitHighlander/keepkey-firmware/pull/836) | `b771669ba` | stack 05 | 2,507 | runtime-provider metadata, transport and display interfaces |
| 07 / [#837](https://github.com/BitHighlander/keepkey-firmware/pull/837) | `4695a1144` | stack 06 | 10,253 | ERC-7730 core and streaming machinery |
| 08 / [#838](https://github.com/BitHighlander/keepkey-firmware/pull/838) | `dc8c80ec9` | stack 07 | 5,471 | ERC-7730 display program and recursive binding |
| 09 / [#839](https://github.com/BitHighlander/keepkey-firmware/pull/839) | `d0c1a7c81` | stack 08 | 291 | authorization, storage and protocol audit helpers |
| 10 / [#840](https://github.com/BitHighlander/keepkey-firmware/pull/840) | `568ee5cb1` | stack 09 | 319 | unknown-token and EIP-712 dimension disclosure |
| 12 / [#842](https://github.com/BitHighlander/keepkey-firmware/pull/842) | `19b228351` | stack 10 | 667 | runtime trust lock plus Hive and Zcash audit carryforward |
| 13 / [#843](https://github.com/BitHighlander/keepkey-firmware/pull/843) | `6875f15d5` | stack 12 | 641 | session boundaries, wire fixes and final convergence |

Every active adjacent diff is nonempty and below 20,000 changed lines. Review counts
are additions plus deletions from `git diff --numstat <base>...<head>`.
Every head is now a direct descendant of its declared adjacent base. Therefore
the branch tree, GitHub pull-request merge tree, tested tree, and reviewed
two-dot snapshot diff cannot diverge through synthetic merge resolution.

PRs #844 and #845 are the two foundation slices. #841 became tree-empty after
the lost audited foundation was restored: its Solana schema/text work is
already in stack 00b. It is therefore retired, and #842 is based directly on
stack 10. This avoids a vacuous review unit without moving or dropping code.
The table records the code head that produced the local Docker evidence; this
handoff update is its docs-only descendant.

## Companion repository boundary

The canonical companion remains the single multi-release
`reconcile/upstream-sync` line. The exact locally validated commit is
`49d537ce953b7524599bfc87f56b7a50a51a13a1`; it adds corrected canonical wire,
OLED and test-isolation evidence plus explicit partial-stack capability gates
without creating a 7.15-only branch. With no capability environment variable,
the same canonical branch enforces the complete release behavior.

The superseded release-only python-keepkey PR #225 was closed and its temporary
remote branches were deleted. Every active gitlink pin belongs to the canonical
multi-release line; the final slice returns to the exact final pin above. Audit
each gitlink change as code: a green companion branch does not by itself prove
compatibility with each partial firmware slice.

`deps/device-protocol` is also security-sensitive. Commit `dd9c85dc747cf965fb0e7bf49615dc9e7568ee65`
is published on `reconcile/7.15-release`; compare generated
message bindings and `.options` limits with the firmware decoder bounds.

## Resolved local blockers

### B1 — final canonical integration suite: resolved locally

Run [35543904890](https://github.com/BitHighlander/keepkey-firmware/actions/runs/35543904890)
on final head `6211b7b7ea` passed firmware unit tests, ARM build, dylib tests,
formatting, static analysis, and secret scanning. Its python-keepkey phase
failed: 33 full-suite failures (635 passed, 159 skipped) and eight failures in
the screenshot selection phase (141 passed, 39 skipped, 639 deselected).

Security-relevant failures include:

- runtime signer survives disabling AdvancedMode;
- runtime signer survives session initialization/teardown;
- bytes after an embedded NUL do not change the OLED review;
- Solana verified instruction fields do not all alter the review;
- Solana LUT attestation expectations disagree with the release policy;
- reset/PIN flows disagree with the transactional ceremony implementation;
- authenticator authorization-loss cleanup fails;
- Ethereum off-mainnet native pseudo-address handling fails;
- Osmosis signed-field/canonical-wire assertions fail;
- TON and Omni display-disclosure assertions fail;
- BIP-85 flows and several protection-level expectations fail.

Each failure was triaged rather than broadly skipped. Firmware fixes cover
signer/session revocation, display binding, Solana field disclosure, excluded
certified/LUT policy, setup lifecycle, chain wire guards and nested-confirm
unwinding. Canonical tests were corrected where their fixtures encoded an
impossible Solana ATA shape, the wrong XRPL MemoData field, or abandoned a
valid maximum-amount confirmation. The final Compose harness also executes the
five owned power-cycle storage tests instead of silently skipping them. Final
regular result: **755 passed, 75 deliberate skips, zero failures**.

### B2 — screenshot evidence certification: resolved locally

The original script tolerated pytest failure and gated only on a nonzero PNG
count. That behavior has been removed while retaining screenshots and JUnit
for diagnosis.

The screenshot phase now fails on pytest failure and the report generator
cross-checks captured sequences against its JUnit result. Final result:
**167 passed, 21 deliberate skips, 642 deselected, 1,128 PNGs in 167 manifested
sequences; every declared screen captured**.

### B3 — partial-release capability gates: resolved locally

All slices report firmware `7.15.0`, while capabilities differ across partial
trees. A test gated only on `>= 7.15.0` can demand a later capability from an
earlier slice. Do not solve this with a release-specific python-keepkey branch
or by weakening final-release assertions. Use an explicit capability contract
for staged CI, defaulting the canonical harness to the complete released
capability set when no partial-stack profile is supplied.

The canonical harness reads `KK_RELEASE_MISSING_CAPABILITIES` only for partial
slice CI and retains version gates only where the behavior is genuinely
versioned. Stack 00b declares its pre-existing safe-reset, THOR deposit,
maximum-amount, legacy-router and protected-Ping gaps plus the later unknown-
token, entropy, Osmosis, Ripple, Hive, prompt-unwind and session capabilities.
Stacks 01-04 retain the maximum-amount and later gaps; stacks 05-06 also mark
the not-yet-landed ERC-7730 runtime review; stacks 07-09 retain both EVM review
gaps. Stack 10 removes the EVM gaps; stack 12 also removes Hive. The
`storage-v19-kdf` and `solana-lut-attestation` controls remain explicitly absent
through stack 12 and become mandatory at stack 13. Stack 13 declares no missing
capabilities. No
release-only python-keepkey fork is used. Ten final policy-convergence unit
tests are likewise compiled only in stack 13: the assertions describe behavior
that does not exist in earlier partial trees, and all remain active in the
release candidate rather than being skipped or weakened there.

### B4 — ARM-only compile and stack-reslice regressions: resolved locally

ARM compilation exposed duplicate declarations/scratch state, an unused
contract-confirmation parameter, and a shadowed Maya memo-loop index that host
tests did not reject. Each correction was applied at its earliest owning slice
and propagated forward. The regular ARM build retains 17,316 bytes of SRAM
reserve (16,384-byte floor); bitcoin-only retains 38,220 bytes.

Reconstructing the corrected 14-slice topology also caught a substantive TRON
regression in the superseded candidate: message display had been truncated and
the typed-hash blind-signing AdvancedMode fence had been lost. The authoritative
tree preserves full-byte `confirm_bytes()` review and requires AdvancedMode for
typed-hash signing. This is why the older green emulator tree is not a release
candidate.

The final workflow audit also removed the non-release `zcash-privacy` CI and
publication leg, made the regular emulator leg exercise the shipped Zcash-
enabled multi-coin product, repaired its publication artifact selection,
added the missing ARM matrix labels, and removed a duplicate `strategy` key
that made `release.yml` structurally invalid. Every workflow passes `actionlint`.
Topic-branch pushes no longer duplicate the same expensive workflow; each
stacked PR base is explicitly included in the `pull_request` trigger, so the
single run validates GitHub's synthetic merge for that adjacent slice.
The intermediate-slice audit also restored the full/bitcoin-only product
defines in stacks 01-04, scoped Hive and Zcash privacy dispatch out of
bitcoin-only, removed duplicate Zcash layout/handler declarations, retained
the checked RNG-health API, moved response-buffer right-sizing to stack 03,
linked the Orchard and constant-time Pallas sources when first required,
versioned the slice-01 RedPallas tests with the pinned crypto API, scoped Zcash
and TRON test suites out of reduced builds, and removed a temporary Zcash
signing-contract regression from stacks 09-10.
The pinned clang-format-20 gate and zero-warning cppcheck gate also pass; the
last cppcheck finding was a duplicated EIP-1559 fee-bound condition removed at
its owning stack. Partial handler bodies at stacks 03-12 retain the mutable
protobuf dispatcher ABI with narrow inline cppcheck suppressions; exact
cppcheck 2.20 runs pass at both stack 03 and stack 12, and the suppressions are
absent from the complete stack-13 source tree.

### B5 — skipped power-cycle storage gates: resolved locally

The ordinary python-keepkey image did not contain a restartable emulator binary,
so U1-U4 in the release report skipped even while the aggregate integration job
passed. Running them manually exposed two stale canonical assumptions: an
unframed V17 record was forced through a CRC-only helper, and the test still
expected 7.15 to erase a newer normal-band record despite the intentional
`SUS_TooNew` non-destructive bridge.

The canonical test now derives the declared wipe-or-refuse policy from the
firmware capability, without a release-specific branch or version-string gate.
The Compose firmware-unit service publishes its exact regular or bitcoin-only
`kkemu` binary through a read-only test volume, allowing python-keepkey to own
and restart the right product. All five power-cycle cases now execute and pass;
the regular full-suite skip count fell from 80 to 75.

## Remaining publication and human gates

1. Verify published python-keepkey
   `49d537ce953b7524599bfc87f56b7a50a51a13a1` remains on the canonical branch.
2. Verify GitHub finishes recalculating every active adjacent PR diff below the
   20,000-line limit.
3. Let the single required GitHub check run on each published immutable head. Local
   Docker evidence does not substitute for protected-branch status.
4. Human auditors review the adjacent diffs, especially stacks 00, 06-08,
   12-13, and compare stack 00 with the previously audited scope-repair tree.
5. Human-authorized hardware testing verifies physical hold/paging, power-loss
   setup boundaries, regular/bitcoin-only installation and artifact identity.
6. Copilot remains disabled unless a human explicitly authorizes it.

## Local validation receipt

Evidence bound to final code tree `9b0583a41b758af7fb4c08280ab2a2bd8bb5d503`
(tree object `1c8e42dadef70ccbebb04a2b5c4630b2dd2cfe23`):

- clang-format 20.1.8: clean;
- cppcheck 2.20.0 with the workflow arguments: zero findings;
- `actionlint .github/workflows/ci.yml`: clean;
- regular and bitcoin-only Docker builds: passed;
- regular native: 597 firmware, 19 board, 18 crypto tests passed;
- bitcoin-only native: 127 firmware, 19 board, 18 crypto tests passed.
- pinned ARM Docker builds emitted 189 regular and 151 bitcoin-only
  `-fstack-usage` records; both SRAM/frame-margin gates passed (17,316-byte and
  38,220-byte reserves respectively).
- intermediate ARM boundary builds passed for both products at stacks 01, 02,
  and 07; regular ARM builds also passed at stacks 03 and 10, while the
  bitcoin-only stack 10 build passed independently.
- updated stack 00b ARM builds passed for regular and bitcoin-only, and its
  canonical Docker integration profile passed 547 tests with 280 deliberate
  capability skips and zero failures;
- updated stack 01 canonical Docker integration profile passed 563 tests with
  264 deliberate capability skips and zero failures.
- exact emulator Docker builds passed for both products at repaired stacks 01
  and 02; stack 01 passed 325 regular and 112 bitcoin-only firmware tests,
  while stack 02 passed 407 regular and 119 bitcoin-only firmware tests, plus
  19 board and 18 crypto tests in every profile;
- the repaired stack-04 bitcoin-only build passed; a later regular stack-06
  Docker build passed 439 firmware, 19 board, and 18 crypto tests, confirming
  the partial-slice gate beyond its introduction point.

Current canonical Docker evidence on that exact tree:

- regular canonical integration: 755 passed, 75 skipped, zero failures;
- regular OLED evidence: 167 passed, 21 skipped, 642 deselected, strict audit
  passed with 1,128 PNGs across 167 sequences;
- bitcoin-only canonical integration: 348 passed, 482 skipped, zero failures;
- bitcoin-only OLED evidence: 40 passed, 148 skipped, 642 deselected, strict
  audit passed with 216 PNGs across 40 sequences;
- stack 12 canonical integration: 723 passed, 104 skipped, zero failures;
- stack 08 canonical integration: 713 passed, 114 skipped, zero failures;
- VULN-1969 followed by VULN-20007 passed in the full final suite, proving the
  protected-Ping workflow is unwound before the successor request.

Earlier regression evidence (must still be regenerated on the published final SHA):

- regular ARM binary: `21a0adf8e0c24834d9de50781edfd7c0cbc309f3dceb3bdd2256cd80c9d56cca`;
- bitcoin-only ARM binary: `069af3797d5d451a01e04ed7d03f324a8ccf42df9bf65442614b990a156661c9`;
- Pallas boundary check passed; fixture manifest SHA-256 is
  `cafd5d9eb14d0186680380bc7c486092d94a547767ec9a5e852ad3a915178d55`;
- focused suite-order regressions: Ethereum maximum-amount refusal successor,
  Osmosis validation/swap paging, VULN-1969 then VULN-20007 all passed;
- `lib/rand/rng_health.c` is present (348 lines); no `lib/` or `include/` file
  is deleted relative to `remotes/bithighlander/audit/715-scope-repair`;
- no firmware verification key exists in `signed_metadata.c`; signer slots are
  RAM-only and AdvancedMode-gated; certified Solana requests fail closed.

## Security audit map

### Trust establishment and revocation

Start with `signed_metadata.[ch]`, `fsm.c`, storage authorization transitions,
and every `LoadClearsignSigner` call path. Trace key, alias, fingerprint,
metadata cache, and source classification together. Test disable/re-enable,
Initialize, lock, wipe, PIN/passphrase changes, malformed load, full signer
table, and cross-protocol reuse. Zeroing only the key while retaining an alias
or source marker is not sufficient.

### Parser and memory boundaries

Review `pb_decode.c`, transport `.options`, `messages.c`, the response/decode
arenas, tiny-json, ERC-7730 ABI streams, recursive display groups, array/path
arithmetic, embedded calls, and signed-metadata length fields. Check integer
overflow before addition/multiplication, exact terminal consumption, aliasing,
use-after-return, recursion limits, and failure cleanup. Compare emulator and
ARM sizes; host-only success is not evidence of MCU safety.

### What is signed versus what is displayed

For EVM/EIP-712/ERC-7730, Solana, TON, TRON, THOR/Maya, XRP, Hive, EOS,
Osmosis, Bitcoin and Zcash, mutate every signed field independently and prove
that the OLED changes or signing refuses. Pay special attention to embedded
NULs, truncation/paging, token contract versus ticker, chain ID, recipient,
raw amount, fee, calldata tails, array order/dimensions, memos, lookup-table
accounts, and duplicate-output state carried across inputs/outputs.

### Transactional setup and persistent state

For stack 12, prove reset/recovery settings and PIN remain in one zeroized RAM
owner until successful ceremony completion. Exercise cancellation, failure,
Initialize, cross-ceremony messages, stale acknowledgements, repeated entry,
power interruption boundaries, U2F counter preservation, and bitcoin-only
builds. No partially staged value may leak into persistent storage.

### Product and build boundaries

Build regular and bitcoin-only products from clean submodules. Confirm the
regular product contains intended chains, bitcoin-only excludes handlers and
data rather than merely hiding UI, and no obsolete zcash-privacy artifact is
published. Audit dense dispatch-table indexing, linker/arena limits, generated
token byproducts, crypto configuration changes, and release artifact naming.

### Cryptographic and recovery paths

Audit app/boot signature verification for duplicated checks and fault
resistance, canonical Hive signatures and derivation paths, Zcash account
aliasing/export consent, BIP-39 per-word validation, BIP-85 display-only
behavior, and secret/scratch zeroization on every exit.

## Suggested audit order

1. Verify the four exact repository SHAs and two image IDs above.
2. Review stack 00 against the 7.14.x audit receipts and scope-repair tree;
   confirm the apparently moved work is present rather than silently dropped.
3. Review stacks 01-05 for chain, recovery, storage and product boundaries.
4. Review stacks 06-08 as one trust/parser subsystem, while retaining the
   adjacent-diff ownership boundaries.
5. Review signer lifetime, authorization loss and excluded certified/LUT paths
   in stacks 09-13 before lower-risk formatting or harness changes.
6. Review every signed-versus-displayed mutation test and its captured frames.
7. Re-run both products, canonical integration and the strict screenshot audit
   from clean recursive submodules.
8. Verify the canonical companion is published, then publish the 14 active
   firmware slices in order.
9. Run protected GitHub checks once per immutable published head; preserve all
   JUnit, screenshot, binary and size artifacts.
10. Perform human-authorized hardware/power-loss testing and record residual
    risks before approving merge or release.

## Reproduction commands

```sh
# Stack topology and live status
gh pr list --repo BitHighlander/keepkey-firmware --state open \
  --json number,title,baseRefName,headRefName,headRefOid,mergeable,mergeStateStatus

# Review one adjacent slice
git diff --stat <adjacent-base>...<head>
git diff --check <adjacent-base>...<head>
git log --oneline <adjacent-base>..<head>

# Historical failing run (diagnostic comparison only; not current evidence)
gh run view 35543904890 --repo BitHighlander/keepkey-firmware --log-failed
gh run download 35543904890 --repo BitHighlander/keepkey-firmware \
  --name python-test-results --dir <new-empty-directory>

# Verify canonical companion ancestry
git -C deps/python-keepkey fetch origin reconcile/upstream-sync
git -C deps/python-keepkey merge-base --is-ancestor \
  49d537ce953b7524599bfc87f56b7a50a51a13a1 \
  origin/reconcile/upstream-sync
```

Use immutable SHAs in audit notes. Do not rely on branch names after a restack.
Do not merge an upper stack PR before its base PR. After each merge, retarget or
verify the next PR and confirm that its adjacent diff remains unchanged.

## Exit criteria

The handoff is ready for human-authorized audit/testing only when:

- B1-B3 are resolved with focused regression tests;
- all 14 exact PR heads have successful required checks;
- the final canonical full suite has zero failures and exactly the reviewed
  capability skips recorded above;
- screenshot evidence is tied only to passing selected tests;
- regular and bitcoin-only artifacts build from clean recursive submodules;
- all companion gitlinks are canonical and immutable;
- no KeepKey trusted root, persistent signer, or warning-free runtime-provider
  path is present;
- the final heads, artifact digests, test totals, exclusions, and any accepted
  residual risks are recorded here before human authorization.
