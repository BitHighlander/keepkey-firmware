# 7.15 release Stack 01 audit — in progress

This is an audit intake, not a completed checkpoint or release decision.

## Identity and scope

- Owner: Codex `/root`, `keepkey-vault-v11-agent-2`; main-worktree claim:
  `docs/release/audit-claims/715-01/OWNER.md`.
- Main-worktree master form SHA256 at intake:
  `0821e9ea94225ff6332a9ce55ac32eb518277eaf0dc50cd2ba3c1f36be0aa131`.
- Isolated worktree: `/private/tmp/kk-715-stack01-audit`, branch
  `audit/715-stack01-review`.
- Code-bearing [PR #831](https://github.com/BitHighlander/keepkey-firmware/pull/831):
  `release/715-stack-01-evm` at `66ca726662822a9ff102bcd1354cc6a39d5a203b`
  against immediate predecessor `release/715-stack-00b-release-foundation` at
  `4c56e19e3070772a28b2890a4ec862ecf77d5429` when claimed.
- The adjacent diff is 80 paths, 3,017 added and 663 deleted lines, from
  `git diff --numstat 4c56e19e3070772a28b2890a4ec862ecf77d5429..66ca726662822a9ff102bcd1354cc6a39d5a203b`.
  It changes firmware, tests, CI, build inputs and documentation; the PR is
  code-bearing. The one commit in the range is `66ca72666`.
- Stack 00b is separately claimed and has no completed checkpoint in the
  canonical ledger. Stack 01 cannot be called accepted on predecessor ancestry
  until that claim completes and its head is reconciled.

## Evidence checked

- `git diff --check` on the frozen range passed.
- Exact-head nonpublishing [CI run 35685552039](https://github.com/BitHighlander/keepkey-firmware/actions/runs/35685552039)
  succeeded: format, secret scan, static analysis, submodule check, full and
  Bitcoin-only ARM builds and SRAM gates, emulator builds, unit and Python
  integration suites, dylib tests, report generation, evidence and CI gates.
  `publish-emulator` was skipped as intended. A concurrent run 35685552437 at
  the same SHA was cancelled; it is not the success receipt.
- Source inspection so far covered the changed reset/dice derivation and pager,
  transport rejection, USB buffer cleanup, FSM session policy, storage CRC,
  signing input comparison, Ripple, MAYAChain, and CI wiring. The complete
  changed-path ledger, dependency pin reconciliation and physical display
  review remain open.
- The local emulator fix and its focused `BodyFits.EmulatorPollAdvancesHostWaitProgress`
  regression passed in the SOP Docker firmware-unit compose run on 2026-09-23:
  327 firmware, 20 board and 18 crypto tests passed. The first local build
  attempt stopped before compilation because the nested pinned `ethereum-lists`
  submodule was absent; after initializing it at `89a64f71`, the same command
  completed with exit code 0. The local fix commits are `cbc725dc6` (pump)
  and a pending test/report commit; these are not in PR #831's green hosted run.

## Open finding and policy disposition

### S01-01 — unrelated host traffic extends an active session's auto-lock

Severity: P2 policy/security review, pending owner decision. Changed lines:
`lib/firmware/fsm.c:328-330` and `lib/firmware/home_sm.c:193-197` at the PR
head. `fsm_usb_rx()` calls `note_host_activity()` before parsing or dispatching
every frame. While `home_state == AWAY_FROM_HOME`, that clears the idle timer
regardless of message type, validity or whether it advances the signing/setup
workflow. A host that periodically sends `GetFeatures` (or malformed frames)
can keep an otherwise stalled signing session from reaching the configured
auto-lock delay. The new tests in `unittests/firmware/fsm.cpp` call
`note_host_activity()` directly and prove only that any call extends the timer;
they do not exercise unrelated frames through USB or set a policy for them.
The same concern was recorded in the 2026-09-13 re-audit handoff for the
candidate line. Decide which validated workflow messages count as progress,
then test both an allowed stream and unrelated/invalid traffic at the RX path.

### S01-02 — emulator progress animation stopped while polling the host

Severity: P3, fixed locally in this audit branch. The PR replaced the tail of
`lib/board/udp.c:usbPoll()` with a buffer scrub and removed its call to
`layout_animate_poll()`. The hardware USB poll still calls the animation pump;
without the emulator call, a queued Zcash progress animation freezes whenever
the emulator is waiting for host proof generation. The local patch restores the
animation call after the buffer scrub. A new board test arms the real progress
trickle, calls emulator `usbPoll()`, and requires a display refresh; it passed
in the local Docker run above. Physical Zcash signing is still a release gate.

## Remaining work

1. Complete source and interaction review for every changed path, including
   message-map parity, host capability pins, the dice verifier, build/release
   scripts and all tests that claim to exercise the runtime behavior.
2. Resolve S01-01 or record an explicit release-owner policy decision and
   impact; retain the existing storage power-interruption risk as inherited
   release blocker.
3. Reconcile Stack 00b's accepted predecessor head, current PR base/head and
   exact dependency pins. Refresh checks for any changed code-bearing head.
4. Finish the master template's historical inventory, report PDF, local
   preflight and authorized late review checkpoint. No Copilot request has been
   made for this audit.
