# 7.15 Stack 07 audit intake — provisional

This is an intake record, not a frozen audit report or acceptance receipt. The canonical SOP and master ledger were read in the main firmware worktree before the `715-07` claim. Main ledger SHA256 at this intake was `67caefef586d17212874072019de8d434e16a79b4edbc52c6e1b7d338d0b87a1`. The main claim is `docs/release/audit-claims/715-07/OWNER.md`.

## Identity and predecessor

Historical [Stack 07 PR #837](https://github.com/BitHighlander/keepkey-firmware/pull/837) is one code-bearing commit `4695a11444fa6a3bb990e59d000b8149bc5b74f3` against reported Stack 06 base `b771669ba842d83790a69b6132f63cea82e5a861`. Its 38-file adjacent diff contains 10,046 additions and 207 deletions. The original unit adds ERC-7730 catalog verification, recursive ABI decoding, a bounded display-program interpreter, EIP-712 streaming, and firmware/unit-test wiring; the Bitcoin-only build omits these sources. No dependency pointer changes in this unit.

Stack 06 is claimed by another agent and is unaccepted. For integration diagnosis only, the original Stack 07 commit was cherry-picked onto that agent's intake head `60b04c46996dda7ebc27555651b3205b852ea2a0`, producing `632de4f7f`, followed by historical-handoff correction `1679e17dca22dd475b8530d292d741403bc18763` on branch `audit/715-stack07-provisional`. This is a **provisional** predecessor, not a proposed final review base. The historical PR was not force-pushed or retargeted.

Two conflicts were resolved during the provisional replay:

1. `.github/workflows/ci.yml`: Stack 05 had moved the staged-capability inventory to workflow scope. Remove only `erc7730-runtime-review` there, so both integration and generated-report jobs see the same remaining omissions. Do not restore the historical job-scoped inventory.
2. `lib/firmware/ethereum.c`: preserve Stack 07's mandatory ordinary amount and raw-calldata review after runtime metadata. The provisional Stack 06 version could suppress screens under its older metadata branch. No firmware-trusted signer is present in this 7.15 unit.

## Security boundary and evidence

| Contract | Current source path and observer | Verification and disposition |
| --- | --- | --- |
| Runtime authority | `erc7730_catalog.c` verifies the catalog-domain attestation against an explicitly loaded runtime signer; `fsm_msg_ethereum.h` gates definition loading and signing entry on AdvancedMode. The user observes signer identity/confirmation and each display screen. | Native catalog tests cover signature, proof, identity, malformed envelope, sequence and resource limits. The runtime signer remains session scoped under Stack 06's separate contract. The accepted-base integration and physical OLED view are pending. |
| Signed bytes and displayed values | `erc7730_abi.c`, `erc7730_abi_stream.c`, `erc7730_program.c`, `erc7730_workflow.c` parse canonical calldata and reject invalid offsets, padding and lengths. The host supplies chunks; the device parses the exact bytes later sent to signing. | New unit tests cover chunk boundaries, recursive values, malformed dynamic layout, replay and continuation. The source aborts on failed definition/capture/replay. Provisional native tests passed; no complete device end-to-end ERC-7730 catalog fixture is established by the historical PR's CI artifact. |
| Additive review | `fsm_msg_ethereum.h` confirms the intent and any supported field before resuming ordinary Ethereum signing; `ethereum.c` retains amount, raw-calldata and fee confirmation for runtime metadata. The user sees both annotation and baseline review. | The provisional conflict was resolved in favor of mandatory baseline review. Host `test_vuln1969` fails on the historical head; Stack 06's guarded prompt diagnostics also remain open. Explicit device display/abort tests and accepted-base CI are pending. |
| Product separation | `lib/firmware/CMakeLists.txt` and `unittests/firmware/CMakeLists.txt` add ERC-7730/EIP-712 only under `NOT KK_BITCOIN_ONLY`; the BTC message map excludes non-Bitcoin handlers. | Historical and provisional regular/BTC native and ARM builds pass. No physical product image was signed or flashed. |
| Bounded subset | Firmware display continuation accepts a bounded intent, raw field formatter/path, and terminal instruction. Unsupported program operations fail closed. | The historical `HANDOFF-ERC7730-ALPHA.md` was corrected to label its full-v2 conformance goal as a historical alpha design, not a claim that the Stack 07 subset implements all of ERC-7730 v2. Complete registry conformance remains outside this unit. |

An additional EIP-712 binding question remains open: `erc7730_catalog_matches_eip712()` compares the streamed type hash, chain ID and optional verifying contract, but its identity/API has no name, version, salt or full domain-separator constraint. Two typed-data domains sharing those compared facts can select the same catalog definition. The underlying streamed EIP-712 flow still validates and displays the supplied domain and the runtime definition cannot suppress baseline review, so this is a bounded annotation/intent risk rather than evidence of a hidden signature. The final audit must either extend the binding for definitions that claim those domain constraints, or explicitly restrict and test the supported subset. Do not report full ERC-7730 EIP-712 domain conformance from the current matcher.

## Executed verification

Pinned builder: `kktech/firmware@sha256:7438e53933d47d53157ed6d96d864cb208597e62dce26235ace09d1063427fa2`. Top-level pins: crypto `8a392f70a5d5575ece3dfb35f115d4a4b27f497c`, protocol `dd9c85dc747cf965fb0e7bf49615dc9e7568ee65`, Python host `49d537ce953b7524599bfc87f56b7a50a51a13a1`, GoogleTest `7888184f28509dba839e3683409443e0b5bb8948`; nested Ethereum list `89a64f717e1690bb31adb3e4c38e23640357333c`.

| Source | Regular | Bitcoin-only |
| --- | --- | --- |
| Historical `4695a1144` native | firmware 539, board 19, crypto 18, Zcash crypto 72 passed | firmware 121, board 19, crypto 18 passed |
| Provisional `1679e17dc` native | firmware 543, board 19, crypto 18, Zcash crypto 72 passed | firmware 122, board 19, crypto 18 passed |
| Provisional ARM MinSizeRel | linked; ELF SHA256 `59dc85e4247e10b9e5a21edfeb031f789c637c90e214909a6e55b54d94290ce4` | linked; ELF SHA256 `447bc9b1eaef94cab0a22d6ce3662b4b1cdcbc5d11bb9f55a88bc0869a1e22f2` |

`actionlint .github/workflows/ci.yml` and `git diff --check 60b04c46996dda7ebc27555651b3205b852ea2a0..1679e17dca22dd475b8530d292d741403bc18763` passed. The native sources register 98 new `TEST` cases across the nine ERC-7730/EIP-712 test files; the regular test binary ran them as part of its 543 tests. BTC excludes those files.

Historical [CI 35685552287](https://github.com/BitHighlander/keepkey-firmware/actions/runs/35685552287) on `4695a1144` passed native and ARM jobs but failed both host jobs and generated-report/final gates. Downloaded JUnit from both product variants names one failure each: `tests.test_vuln1969.TestVULN1969.test` timed out on `Initialize` after rejecting an interrupt during a protected Ping. The merged historical JUnit also fails the 7.15 validator for the three staged storage KDF checks when no inventory is available; the historical report job did not inherit the integration job's inventory. The accepted Stack 05 workflow-scope repair is preserved in the provisional replay, but **no hosted CI has run on that provisional head**.

As a targeted report-gate check, the downloaded historical merged JUnit fails the 7.15 validator without a capability inventory (three staged storage KDF checks missing and four Solana LUT checks skipped-but-required) and **passes** with the exact provisional workflow's shared `KK_RELEASE_MISSING_CAPABILITIES` value. This validates the inventory plumbing, not the failed host test or a hosted exact-head report artifact.

## Findings and next gate

- **Fixed in provisional source:** the historical alpha-only handoff incorrectly stated that this work did not change 7.15. Commit `1679e17dc` qualifies it as an older full-conformance plan and identifies Stack 07 as a bounded 7.15 extraction.
- **Pending predecessor:** Stack 06 must complete its guarded prompt/session/Ripple controls and publish an accepted head. Rebase this unit onto that head, inspect the complete adjacent diff, and rerun affected tests. The present branch is not a substitute for the accepted predecessor.
- **Pending host/report gate:** reproduce and resolve the `test_vuln1969` prompt timeout on the accepted chain; verify the report gate with a shared explicit staged-capability inventory. A green native suite does not close these failures.
- **Pending EIP-712 scope:** the catalog matcher omits name/version/salt/full-domain binding. Decide the supported subset and add a negative test for same type/chain/contract with a different domain before claiming an exact EIP-712 descriptor match.
- **Pending final audit:** run matching-emulator host tests for the new catalog path, exact-head hosted CI, ARM artifact checks, complete skip inventory, final source/PDF report, code-bearing preflight, and authorized late review. No Copilot request is registered for Stack 07.

## Provisional retrospective

The historical Stack 07 PR was published before Stack 05's report-gate repair and before Stack 06's accepted audit. Replaying it immediately revealed a workflow inventory conflict and a security-significant choice about raw review. Resolving those deliberately on a disposable branch avoided treating a clean native result as release readiness. The audit should carry forward the immutable test and source evidence while replacing this provisional predecessor with the accepted Stack 06 head before the final report and review.
