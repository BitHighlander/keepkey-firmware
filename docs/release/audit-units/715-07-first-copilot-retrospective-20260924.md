# Stack 07 first Copilot review retrospective — 2026-09-24

## Exact scope and result

The owner directed one immediate Copilot request on historical, code-bearing [PR #837](https://github.com/BitHighlander/keepkey-firmware/pull/837), base `b771669ba842d83790a69b6132f63cea82e5a861`, head `4695a11444fa6a3bb990e59d000b8149bc5b74f3`. The request registered at 2026-09-24T02:20:47Z. [Review 5298993214](https://github.com/BitHighlander/keepkey-firmware/pull/837#pullrequestreview-5298993214) arrived at 02:29:47Z as **Changes recommended** with 13 inline comments and an overview labeling 11 findings. The actual body and all 13 comments were read. This is one review request; no second request is authorized. All threads remain open pending pushed fixes or technical replies.

The review is provisional: Stack 06's accepted predecessor is still unknown, historical Stack 07 CI failed host and report gates, and the final adjacent Stack 07 diff must be rebuilt on the accepted Stack 06 head. The local `audit/715-stack07-provisional` branch is an integration rehearsal, not PR #837's reviewed head.

## Triage of every inline comment

| Comment | Initial disposition and evidence | Next action |
| --- | --- | --- |
| `4089197347`, public EIP-712 header guard | **Confirmed.** `#endif` precedes public walk typedefs/declarations. | Move guard to file end; compile a double-include negative control. |
| `4089197364`, reviewed calldata may differ from signed stream | **Confirmed.** Workflow parses the same ABI shape on first pass and later signing, but holds no digest or byte equality commitment. | Bind every replay and signing stream to a digest of the first validated bytes; test same-shape changed value refusal. |
| `4089197391`, array-level count read before bound | **Confirmed.** `eip712_type_name()` indexes `array_levels` before the state machine's later count check. | Check count against wire capacity at function entry and test oversized count. |
| `4089197421`, nested fixed array dimension | **Confirmed.** `drive_array_element()` sets `pending_declared_dim = 0` for inner dimensions. | Carry each declared dimension and test `uint256[2][3]` with wrong inner length. |
| `4089197450`, empty root tuple | **Confirmed for bounded subset usability.** The generic ABI validator rejects `child_count == 0`, making a valid selector-only call unavailable. | Accept canonical empty root tuple and exact zero-byte argument block; test it. |
| `4089197469`, negative index signed overflow | **Refuted as stated.** `wanted` is `int32_t`, the added `child_count` is bounded by `uint16_t`/`ERC7730_ABI_MAX_ARRAY_ELEMENTS`, and adding a nonnegative bounded count to `INT32_MIN` cannot underflow or overflow. The result remains negative and fails the index check. | Record this arithmetic proof in the thread after the final source head is published; no code change solely for this claim. |
| `4089197498`, signature does not bind header/certificate context | **Refuted for transaction identity fields.** The signed Merkle leaf hashes every program byte, including the 179-byte header containing chain, contract, type/selector, provider and epochs. The certificate's public key must be the same loaded runtime signer that verifies the signature, and its scope must equal the signed header chain. The host-supplied definition ID is checked against the complete envelope hash; it is not an authority by itself. | Add a focused tampered-header/certificate negative test if feasible, then document the proof in the thread. |
| `4089197515`, catalog rejects empty tuple | **Confirmed; same root cause as `4089197450`.** The streamed catalog validator also requires `child_count != 0`. | Fix alongside generic ABI validator; test authenticated parser shape. |
| `4089197543`, streamed EIP-712 bypasses AdvancedMode | **Policy/scope clarification needed.** The ordinary device-driven stream validates, displays and hashes every leaf, unlike blind precomputed-hash signing. The ERC-7730 certified overlay is explicitly gated on AdvancedMode in `fsm_msgEthereumSignTypedData`. The PR title/body overstate the scope by saying all EIP-712 streaming is behind AdvancedMode. | State the separate ordinary-stream contract in the Stack 07 report and PR body; test the policy boundary. Do not silently change the product policy based on the overview alone. |
| `4089197567`, only first certified typed-data display field | **Confirmed.** The EIP-712 flow starts one capture and aborts the workflow at `EIP712_REQ_DONE`; the catalog permits multiple field instructions. | Iterate and confirm every field, or reject multi-field EIP-712 programs during authenticated validation. Until fixed, no complete clear-sign claim. |
| `4089197583`, new typed-data entry bypasses AdvancedMode | **Same policy question as `4089197543`.** A preloaded certified definition requires AdvancedMode; ordinary streamed EIP-712 is separately reviewed. | Clarify and test rather than treating the two endpoints as one policy. |
| `4089197600`, chain ID maximum dropped | **Confirmed defense-in-depth gap.** Public FSM validates the full range, while `ethereum_signing_init()` now checks only nonzero for direct callers. | Restore `ethereum_chainIdIsValid(msg)` and test the maximum boundary. |
| `4089197619`, alpha-only handoff contradicts 7.15 | **Confirmed on historical reviewed head, fixed in local provisional commit `1679e17dc`.** It now labels the alpha plan as historical and distinguishes bounded Stack 07 extraction. | Carry that correction to the final accepted-base PR and reply with the pushed SHA. |

## Why this review failed and what changes

The historical PR staged roughly ten thousand changed lines before a source-level adversarial pass, and the unit tests concentrated on parser internals. They did not establish equality between reviewed and signed calldata, bound every typed-data display field to the UI, or verify the policy story expressed by the PR title. The review also caught inexpensive header and range regressions. At least three comments are overlapping or technically incorrect, so the retro records source-based dispositions rather than treating the review label as a count of independent defects.

The next local pass should batch fixes for the confirmed issues, add negative tests at the same trust boundaries, and re-run both product variants. Keep the historical comments open until fixes are pushed to the reviewed PR or until exact technical declines are posted. Reconcile onto Stack 06's accepted head, freeze the final report and pass exact-head CI before asking the owner about another Copilot request. A clean native suite or a thread resolution alone is not a clean review.
