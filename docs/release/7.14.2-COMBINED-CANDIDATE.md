# 7.14.2 canonical integration candidate

Status: full-diff audit complete, 0 findings open; ready for the Copilot /
multi-agent review phase. Branch `audit/7142-full-audit-fixes`, PR
[#759](https://github.com/BitHighlander/keepkey-firmware/pull/759), unmerged
into fork develop. This document is not a claim of perfect firmware. The current program is controlled by the
[fork rehearsal SOP](https://github.com/BitHighlander/keepkey-firmware/blob/audit/releases-p00-scope/docs/release/REHEARSAL-SOP.md)
and its phase ledger, not older copied acceptance claims.

## Exact candidate

How this candidate was assembled, as history:

- Canonical starting head: `dedfd7e407a2e17a7a6e7312fb86d7a4deb32b54` (B01).
- Batch B02 head: `b08a68717c46af3204457149580986fe2682a707`.
- Assembly merge: `e21082da1729e71be2ef1edc2db3ab6d60f5aea5`, tree
  `f15b642b0e37a6fb9923adbc16efb9296f22ac2b`, which carried B01's receipt into
  the tested audit stack without conflict.

**None of the heads above is the validated head.** The audit that followed
changed code on this line; the head this candidate is accepted at, and the CI
run that proves it, are in "Accepted internal receipt" below, and the pins it
actually carries are under "Dependency pins" -- both regenerated from the tree
rather than copied forward.

## Accepted bounded batch

| Units / commits | Behavior and acceptance |
| --- | --- |
| P03 durability, `75be71abc`, `63037ea11` | Preserve the old record until a verified replacement exists; retain V17 offsets and generation/CRC framing; recover sampled pending/torn commits without wiping the wallet. |
| P04-001, `5c9d8a3ab` | Hash exactly the 32-byte transaction ID for warning history. |
| P04-002, `d4c23c9d5` | Retain the same input digest across outputs and reset current state at signing start/abort. |
| P04-003, `ebf0eb500`, `875af590e` | Preserve leading wallet components during mixed-script change classification; portable regression include verified in Linux CI. |
| P03 bootloader evidence, `c1f33fcbb`, merge `6bf2f396e` | Optional export of fixed-wallet commit images; no production test hook or dependency change. |
| P04-004, `b08a68717` | Refused duplicate outputs cannot replace accepted history and authorize a retry; repeated refusal and original-input recovery controls pass. |

Reviewed the production and regression deltas and their affected consumers:
metadata offsets and CRC range, active/pending selection, marker ordering, legacy
fallback, storage initialization/commit, signing lifecycle, change classification,
and confirmed/refused output history. Test-only snapshot export is opt-in and
absent from hardware code. No unrelated alpha feature is included.

The released bootloader 2.1.4 payload from upstream v7.3.2 matches the firmware's
recognized double-SHA256
`fe98454e7ebd4aef4a6db5bd4c60f52cf3f58b974283a7c1e1fcc5fea02cf3eb`.
Its unmodified ARM storage-protection decision routines preserve all 17 captured
commit/sample-interruption images. The runner also passes 799 controls covering
empty storage, valid/missing markers and every marker bit in all three sectors.
Only peripheral erasure is intercepted to record intended sectors. This is binary
logic evidence, not a complete hardware boot, other bootloader binaries or
exhaustive power-failure proof. Existing firmware recommends updating older known
bootloaders; that policy is unchanged. Physical upgrade validation remains a
release gate. The runner and snapshot hashes are in the SOP branch P03 ledger.

## Final batch validation

[CI 34412006017](https://github.com/BitHighlander/keepkey-firmware/actions/runs/34412006017)
succeeded on the exact validated code head, including format, static analysis,
secret scan, ARM, native/host, dylib, report and aggregate evidence gates.
Publication was disabled. Independently inspected downloaded artifacts:

- Native: 164 firmware, 28 board and 4 crypto cases pass, zero skips/failures/errors.
- Host: 434 pass / 47 skip; screenshot JUnit 77 pass / 7 skip; dylib 4 pass.
  Zero failures/errors in each JUnit. Skips retain their existing capability and
  transport dispositions; they are not positive coverage.
- All 23 ARM artifact hashes match the manifest, which binds the exact firmware
  and host pins. Application ELF SHA256:
  `0bd957113957713f49391c8f0f910a754c1e2ac6ddd8a7a69fa4a20d8962b78e`.
- `_stack - _ebss = 22,500` bytes; the 16,384-byte floor is retained.
  The emulator snapshot hook is absent from the ARM symbol table.

Local evidence: `/private/tmp/7142-assembly-b02-evidence` and
`/private/tmp/7142-rejected-history-{native,host-preflight}.log`.
The older failed ebf0eb500 Linux test build is superseded by the explicit
<cstring> correction and successful exact-head validation; it is not hidden.

## Remaining release gates

Complete the reconciled release-surface review, bind inherited review identities,
close remaining phase findings and cross-phase interactions, and validate the
final assembled release. The current prospective inventory has 353 paths and
most remain pending. This batch is integrated hardening, not full audit completion.
The later pending-version refusal work applies to 7.14.3/7.15's Bitcoin-only band;
7.14.2 intentionally retains its unbanded version policy. No new band is imported.
No Copilot, develop merge, upstream submission, signing or publication is authorized
by this receipt. Physical OLED and signed-upgrade gates remain external deliverables.

## Dependency pins

- `code-signing-keys`: `a6470bd8598e5e9a7bfc38bf139a5e5a616f05ec`
- `deps/crypto/trezor-firmware`: `03d8a55a832fb61bb89477ef7239a80ecb367080`
- `deps/device-protocol`: `f2c3c005ad824df11aec9592a8b62066323ba282`
- `deps/googletest`: `7888184f28509dba839e3683409443e0b5bb8948`
- `deps/python-keepkey`: `7f538a95f00fdea59879ceab1a684762fe408411`
- `deps/qrenc/QR-Code-generator`: `6dfbfdad5d9303ed190d1c3cb7bec34b565b6ce8`
- `deps/sca-hardening/SecAESSTM32`: `71d356a1141624994cf613bd2d2583892e8e6d5a`

Every `.gitmodules` branch key names a branch verified to contain the pin
above, so `git submodule update --remote` cannot rewind a release pin.
`deps/python-keepkey` tracks `reconcile/upstream-sync`, the canonical host
branch; the key previously said `master`, which does not carry this pin.

## Accepted internal receipt

Validated code head: `61fd0735c3bcc1a2541d4151e1025a54b17786c0`.
Validated tree: `6555abb51a4affd58d0438c898539b5397146825`.
[CI run 34709136785](https://github.com/BitHighlander/keepkey-firmware/actions/runs/34709136785)
passed every required job on that exact head: formatting, static analysis,
secret and submodule checks, ARM builds with the runtime-SRAM link gate,
emulator builds, native unit suites, host integration, the shared-library
suite, generated-report validation and the CI gate. `publish-emulator` is
workflow_dispatch-only and is skipped, as designed.

Host results on that run: 959 cases, 713 passed, 246 skipped, 0 failed, 382 OLED
frames captured. Pinned python-keepkey for that run: `7f538a95f00fdea59879ceab1a684762fe408411`.

A skip is not a pass, so the skips are itemised rather than summed. The
largest reasons on this run:

- 188: Firmware version 7.15.0 or higher is required to run this test
- 14: Firmware version 7.14.3 or higher is required to run this test
- 11: this firmware tree predates the storage version gate: storage.h defines no STORAGE_VERSION_LAST_S...
- 8: Firmware does not report supports_taproot
- 7: ZcashGetOrchardFVK not supported by this firmware build
- 5: no kkemu binary to start and stop

The evidence bundle (PDF, merged JUnit, manifest, checksum) is the
`test-report` artifact of that run and binds this firmware SHA and this
python-keepkey SHA.

This receipt is documentation only: the validated head named above is this
commit's parent, and the commit carrying these words changes no code, no test
and no pin. A reader checking the claim should diff this commit to see that.

## Full-diff audit, 2026-09-12

This candidate was audited by reviewing every changed file against its base,
not by re-reading the summaries above. 249 findings were raised across the
three release lines; the 104 rated P1/P2 were each re-checked by three
independent reviewers along separate lines of attack (is it reachable, does it
matter on a shipped release, is it already covered), and 84 survived that.
Everything that survived and applies to this line is fixed at the head below.

Each fix carries a test, and where a control was possible the unfixed code was
built and the test confirmed to fail against it -- a test that passes either
way is recorded as such in the file rather than counted as coverage.

Findings on this line are listed with their resolution in `audit-units/`.
The load-bearing ones for a reader of this receipt:

- cipher recovery committed the EMPTY mnemonic when a ceremony was driven with separators only
- the address confirmation screen showed the first 44 characters of a 62-character bech32m address beside a QR encoding all of it: the second row was drawn off-canvas and dropped silently, and the fix for it had been added to this line and then reverted
- the THORChain deposit decoder was not pinned to a router, so deposit-shaped calldata to any contract inherited the clear-sign UX and skipped the AdvancedMode gate
- the XRP ceiling was 1e6 too low; CACAO deposits rendered at zero decimals
- the commit CRC left the last byte of the V17 record outside write verification while a unit test asserted the opposite
- layout_debuglink_watermark() was deleted while the bootloader still called it, so no DEBUG_LINK device image could be built
- auto-lock measured wall-clock rather than idleness and tore down live ceremonies
- 'Maximum priority fee' could be below what the Solana runtime charges, because the derived compute-unit limit gave the ComputeBudget instructions nothing (SIMD-0170 allocates them 3,000 each) -- the same understatement was open on all three lines

A second pass then re-read the P3s that the first pass had triaged rather than
re-checked, because "read and judged not to warrant a change" is a weaker
claim than the rest of this receipt makes. Two of them were real and are now
fixed on this line: the multisig sighash scratch was one byte short of the
largest signature nanopb can decode, so its own comment's claim to remove an
out-of-bounds write rather than rely on a cap was false; and the transfer
amount screen took the Wanchain ticker from a static the transfer path had not
set, so a previous Wanchain transaction named the asset on the next transfer.
The rest were verified at head -- some already fixed by another finding's
change, some unreachable -- and `audit-units/` records which, so the triaged
list is not mistaken for a list of live defects.

Two defects were found in the audit's own work and corrected the same way: a
router pin that refused Maya deposits (CI caught it), and a `const` violation
that only ARM GCC rejects (the emulator's clang accepts it). Local ARM
cross-builds using CI's own base image now cover every variant.
