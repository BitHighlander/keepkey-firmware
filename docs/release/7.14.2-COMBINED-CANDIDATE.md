# 7.14.2 canonical integration candidate

Status: batch B02 validated for internal integration; full release audit remains
open. Product PR #650 stays open and unmerged into fork develop. This is not
Copilot readiness. The current program is controlled by the
[fork rehearsal SOP](https://github.com/BitHighlander/keepkey-firmware/blob/audit/releases-p00-scope/docs/release/REHEARSAL-SOP.md)
and its phase ledger, not older copied acceptance claims.

## Exact candidate

- Canonical starting head: `dedfd7e407a2e17a7a6e7312fb86d7a4deb32b54` (B01).
- Validated code head: `b08a68717c46af3204457149580986fe2682a707`.
- Assembly merge: `e21082da1729e71be2ef1edc2db3ab6d60f5aea5`, tree
  `f15b642b0e37a6fb9923adbc16efb9296f22ac2b`.
- The merge carries B01's receipt into the tested audit stack without conflict.
  Its non-receipt diff against the validated code head is empty. This final
  receipt update and historical-SOP notices also change documentation only.
- Dependency pins are unchanged from B01, including host
  `d3b26aee636d6d203fd91e988d1b273c4971a3ab`, protocol
  `f2c3c005ad824df11aec9592a8b62066323ba282`, and crypto
  `03d8a55a832fb61bb89477ef7239a80ecb367080`.

## Accepted bounded batch

| Units / commits | Behavior and acceptance |
| --- | --- |
| P03 durability, `75be71abc`, `63037ea11` | Preserve the old record until a verified replacement exists; retain V17 offsets and generation/CRC framing; recover sampled pending/torn commits without wiping the wallet. |
| P04-001, `5c9d8a3ab` | Hash exactly the 32-byte transaction ID for warning history. |
| P04-002, `d4c23c9d5` | Retain the same input digest across outputs and reset current state at signing start/abort. |
| P04-003, `ebf0eb500`, `875af590e` | Preserve leading wallet components during mixed-script change classification; portable regression include verified in Linux CI. |
| P03 bootloader evidence, `c1f33fcbb`, merge `6bf2f396e` | Optional export of fixed-wallet commit images; no production test hook or dependency change. |
| P04-004, `b08a68717` | Refused duplicate outputs cannot replace accepted history and authorize a retry; repeated refusal and original-input recovery controls pass. |

Reviewed the production and regression deltas and their affected consumers:
metadata offsets and CRC range, active/pending selection, marker ordering, legacy
fallback, storage initialization/commit, signing lifecycle, change classification,
and confirmed/refused output history. Test-only snapshot export is opt-in and
absent from hardware code. No unrelated alpha feature is included.

The released bootloader 2.1.4 payload from upstream v7.3.2 matches the firmware's
recognized double-SHA256
`fe98454e7ebd4aef4a6db5bd4c60f52cf3f58b974283a7c1e1fcc5fea02cf3eb`.
Its unmodified ARM storage-protection decision routines preserve all 17 captured
commit/sample-interruption images. The runner also passes 799 controls covering
empty storage, valid/missing markers and every marker bit in all three sectors.
Only peripheral erasure is intercepted to record intended sectors. This is binary
logic evidence, not a complete hardware boot, other bootloader binaries or
exhaustive power-failure proof. Existing firmware recommends updating older known
bootloaders; that policy is unchanged. Physical upgrade validation remains a
release gate. The runner and snapshot hashes are in the SOP branch P03 ledger.

## Final batch validation

[CI 34412006017](https://github.com/BitHighlander/keepkey-firmware/actions/runs/34412006017)
succeeded on the exact validated code head, including format, static analysis,
secret scan, ARM, native/host, dylib, report and aggregate evidence gates.
Publication was disabled. Independently inspected downloaded artifacts:

- Native: 164 firmware, 28 board and 4 crypto cases pass, zero skips/failures/errors.
- Host: 434 pass / 47 skip; screenshot JUnit 77 pass / 7 skip; dylib 4 pass.
  Zero failures/errors in each JUnit. Skips retain their existing capability and
  transport dispositions; they are not positive coverage.
- All 23 ARM artifact hashes match the manifest, which binds the exact firmware
  and host pins. Application ELF SHA256:
  `0bd957113957713f49391c8f0f910a754c1e2ac6ddd8a7a69fa4a20d8962b78e`.
- `_stack - _ebss = 22,500` bytes; the 16,384-byte floor is retained.
  The emulator snapshot hook is absent from the ARM symbol table.

Local evidence: `/private/tmp/7142-assembly-b02-evidence` and
`/private/tmp/7142-rejected-history-{native,host-preflight}.log`.
The older failed ebf0eb500 Linux test build is superseded by the explicit
<cstring> correction and successful exact-head validation; it is not hidden.

## Remaining release gates

Complete the reconciled release-surface review, bind inherited review identities,
close remaining phase findings and cross-phase interactions, and validate the
final assembled release. The current prospective inventory has 353 paths and
most remain pending. This batch is integrated hardening, not full audit completion.
The later pending-version refusal work applies to 7.14.3/7.15's Bitcoin-only band;
7.14.2 intentionally retains its unbanded version policy. No new band is imported.
No Copilot, develop merge, upstream submission, signing or publication is authorized
by this receipt. Physical OLED and signed-upgrade gates remain external deliverables.
