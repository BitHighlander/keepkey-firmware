# 7.14.2 canonical integration candidate

Status: batch B01 validated for internal integration; full release audit remains
in progress. Earlier “internal acceptance passed” and Copilot-readiness wording
in this document is superseded. Product PR #650 remains open into fork develop.

## Batch B01 identity and scope

Canonical starting head: `e546d99798a5e7dbc54ad5fe43b040a2806c8291`.
Validated code head: `02349bf776c92fef3af4a696245f28ff45651313`.
Validated tree: `5a11c2d18b1c344342e40f0da058653a5c8f7599`.
Assembly branch: `assembly/7142-b01`. This receipt is the only change after that
validated code head. Integration preserves the full source tree and ancestry;
no product feature is omitted and no conflict resolution is needed.

Accepted bounded units included, in dependency order:

| Commits | Contract |
| --- | --- |
| `71617e991`, `bedf3aca1` | Preserve displayed Ripple address across shared response-arena use; pinned host address assertion. |
| `70ae2ab8d` | Each debug backup subpage consumes its own acknowledgement. |
| `176cc998d` | Protected Ping reconstructs response presence and value after confirmation. |
| `7b26c58dc` | Aborted or foreign setup ceremony cannot commit or report success. |
| `100f9f2e9` | Loaded language and label fields remain bounded and terminated. |
| `d59e16cd8` | Emulator sector erase clears the selected sector and preserves neighboring bytes. |
| `e33fa4a00` | Current-PIN and wipe-code buffers are scrubbed on every return. |
| `c593bdbd7` | Recovery abort clears the debug autocomplete buffer. |
| `132db57da` | Storage-backed staging isolation and foreign-commit revocation checks. |
| `534ac50a9`, `543b43185` | Tiny receive failure unwinds the suspended handler and suppresses duplicate responses. |
| `02349bf77` | Reset test observes the transition flag independently of the hardening flag. |

Reviewed the complete batch delta and host-pin delta. Interaction checks covered
setup commit callers and success responses, temporary credential cleanup, response
arena lifetime across debug confirmation, tiny cancellation propagation, and
emulator storage tests after real sector erasure. Earlier focused reproductions
and cross-product applicability remain in the P02/P03 finding ledgers on the SOP
branch. This accepts these bounded contracts, not whole-file or whole-phase coverage.

## Exact dependency pins

- `code-signing-keys`: `a6470bd8598e5e9a7bfc38bf139a5e5a616f05ec`
- `deps/crypto/trezor-firmware`: `03d8a55a832fb61bb89477ef7239a80ecb367080`
- `deps/device-protocol`: `f2c3c005ad824df11aec9592a8b62066323ba282`
- `deps/googletest`: `7888184f28509dba839e3683409443e0b5bb8948`
- `deps/python-keepkey`: `d3b26aee636d6d203fd91e988d1b273c4971a3ab`
- `deps/qrenc/QR-Code-generator`: `6dfbfdad5d9303ed190d1c3cb7bec34b565b6ce8`
- `deps/sca-hardening/SecAESSTM32`: `71d356a1141624994cf613bd2d2583892e8e6d5a`

Only the host pin changes from the canonical starting head. Its two-file delta
adds protected-Ping presence/value regression coverage and restores the displayed
Ripple address assertion. No host library implementation changes are included.

## Validation

[CI 34299743827](https://github.com/BitHighlander/keepkey-firmware/actions/runs/34299743827)
succeeded at the exact validated code head: format, static analysis, secret scan,
ARM, native/host integration, dylib checks, report and aggregate evidence gate.
Publication was disabled and skipped. Downloaded artifacts were independently
inspected for this assembly:

- Native: 159 firmware, 19 board, 4 crypto cases; zero failures/errors/skips.
- Host: 481 cases, 434 passed and 47 skipped; zero failures/errors.
- Screenshot JUnit: 84 cases, 77 passed and 7 skipped; zero failures/errors.
- Dylib: 4 cases passed, no skips/failures/errors.
- ARM manifest binds the exact firmware and host heads above; all 23 listed files
  match their SHA-256 hashes. Firmware ELF SHA-256:
  `945114d53323b2f6aeb8e1c0c6fafd656091aeeb2f736da7daab31ff4b2eeba3`.
- ELF symbol inspection: `_stack - _ebss = 22,508` bytes, above the 16,384 floor.

Local artifact directory: `/private/tmp/7142-assembly-b01-evidence`.
Skipped cases are exclusions, not positive coverage. This carries the existing
candidate's capability/skip disposition; full release coverage remains open.
The later d4c23c9 audit CI is additional evidence, not this batch's exact identity.

## Remaining release work

B01 deliberately stops before the storage durability draft. That draft's installed
bootloader compatibility evidence remains open; sampled emulator fault recovery
does not establish execution on an installed bootloader. Later input-history fixes
and the new change-path investigation also remain outside this batch. Their absence
is recorded existing audit debt, not a claim that B01 resolves those defects.

Reconcile the current coverage inventory, complete all pending phases and required
interactions, resolve remaining actionable findings, and integrate subsequent
accepted units before internal release acceptance. Physical OLED/signed-upgrade
release gates remain separate; no physical validation, Copilot review, develop
merge, upstream submission, signing or publication is claimed.
