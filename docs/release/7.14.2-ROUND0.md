# Round 0: initial 7.14.2 evidence and selected hardening

Inspected 2026-09-07 at e519dc13ced6adbd77cc5ceb6207f3cc04e57492.
This is a bounded triage of known findings, not a new whole-firmware audit.

## Baseline evidence

Upstream PR #458 still names this head. GitHub reports successful lint-format,
secret-scan, static-analysis, build-and-test, build-arm-firmware,
python-dylib-tests, generate-test-report and release-evidence-gate checks.
Publication was skipped. Example run: https://github.com/keepkey/keepkey-firmware/actions/runs/33075778273
These are reported check conclusions; underlying test counts, hardware evidence,
recursive submodules and artifact provenance have not yet been independently
reconciled for foundation acceptance. Local Docker is not running.

The latest Copilot review (5040173535) reported no new comments on
`e1f5e99236eaf2c0ba86c0da799af29142a56b3d`, an earlier head. It is useful prior
review evidence, not a clean review of the selected final baseline.

## Known finding dispositions

| Source | Baseline observation | Disposition |
| --- | --- | --- |
| Fork #542 (all three backports) | ethereumFormatAmount checks bn_format; addAuthAccount clears decoded secret and source; reset_init ignores display_random | Fixes already present; no duplicate backport. No tracker mutation performed. |
| Fork #517 / #518 | signing_validate_input bounds signatures to 72; serialize_script_multisig checks capacity; witness assembly uses separate sighash scratch | Reported bounds fixes already present; no duplicate patch. |
| Fork #460 / alpha C3-002 | fsm_getDerivedNode wipes on entry/root failure/derivation failure; fsm_abort_workflows wipes the shared node | Named failure/central-abort defects addressed; this does not certify every caller's success cleanup or U2F. |
| Fork #469 | pull_request bases only master/develop; all checkouts select head.sha instead of merge SHA | Confirmed validation gap; H01. |
| Fork #454 | storage_secMigrate encrypt leaves iv[64] copied from storageKey; storage_cipherBlock encrypt likewise; decrypt AES contexts are also not scrubbed | Confirmed retained key material, existing defect; H02 covers the paired cipher cleanup. No independent USB disclosure primitive established. |

## H01 contract

Trigger PR validation on rehearsal/**, release/** and hotfix/** in addition to
master/develop. Use the event SHA consistently for checkout, ARM manifest and
PDF provenance: a PR event tests its synthetic merge; push/dispatch tests its
commit. Keep read-only permissions and manual-only publication. Validate YAML
and expressions, then verify a real PR run's event, checkout and artifacts.
Do not claim a push-only green run proves the merge result.

## H02 contract

Clear the complete IV scratch and AES expanded-key context after both encrypt
and decrypt paths in storage_secMigrate and storage_cipherBlock. Preserve
ciphertext, storage layout/version, key derivation and authentication behavior.
Prove cleanup with instrumentation at the actual crypto boundary and retain
round-trip compatibility controls; do not test abandoned stack bytes by
undefined reads. Review all exits in the two functions.

## Outstanding foundation work

Finish known-finding inventory (including applicable alpha records), reconcile
baseline artifacts and recursive pins, select any further justified blockers,
and run assembled-candidate acceptance. No feature import, foundation
acceptance, develop reset or upstream merge is authorized by this evidence.
