# 7.14.3 combined fork product candidate

Status: internal acceptance passed; canonical promotion authorized by the program. Product PR
[#627](https://github.com/BitHighlander/keepkey-firmware/pull/627) targets fork
develop and remains unmerged. This document is not a claim of perfect firmware.

## Source and scope

Frozen starting source: `abe29d1288638867dc64dfe04219b89f7a593133`. Candidate branch:
`rehearsal/7143-combined-product`. Retain source ancestry and apply small audit
units; do not merge into develop. The audit-unit receipts in `audit-units/` record
individual contracts, reproduced failures, fixes, and focused checks.

Bitcoin-only is the canonical product. Preserve the reserved storage-version
band and its refusal behavior in full firmware. Validate both build variants
because the source and CI expose both; the named release product is Bitcoin-only.
Shared transport, storage, and teardown hardening applies. Non-Bitcoin signing
engines are compiled out of the Bitcoin-only artifact.

## Dependency pins

- `code-signing-keys`: `a6470bd8598e5e9a7bfc38bf139a5e5a616f05ec`
- `deps/crypto/trezor-firmware`: `cdc05bebe9e6989cf711e1b5bea6324fd09f848e`
- `deps/device-protocol`: `8545cd5b615f5832374afbf06387a3f28869285e`
- `deps/googletest`: `7888184f28509dba839e3683409443e0b5bb8948`
- `deps/python-keepkey`: `7f538a95f00fdea59879ceab1a684762fe408411`
- `deps/qrenc/QR-Code-generator`: `6dfbfdad5d9303ed190d1c3cb7bec34b565b6ce8`
- `deps/sca-hardening/SecAESSTM32`: `71d356a1141624994cf613bd2d2583892e8e6d5a`

## Acceptance gates

- Complete native suites, including restored FSM tests and applicable crypto.
- Full host integration and storage power-cycle tests against this candidate.
- Exact candidate CI: formatting, static analysis, secret/submodule checks,
  ARM build and SRAM floor, unit/integration variants, evidence aggregate gate.
- Resolve every known applicable finding. Record source-policy conflicts and
  exclusions explicitly; do not replay superseded behavior or weaken tests.
- Final physical OLED and signed-upgrade checks remain release-signing gates.
  Emulator display artifacts do not substitute for a physical device.

Only advance the canonical branch after candidate CI and applicable internal
checks pass. Record exact code head, workflow run, artifact provenance and skips
before declaring readiness for the final Copilot phase. Do not request Copilot
inside this internal rehearsal or automatically publish emulator artifacts.

## Accepted internal receipt

Validated code head: `36ea6cfbf96fc7c3ccfcd24fe0a0ce8c0643d8b9`.
Validated tree: `b6fff4b2bb5860d9ae6a3fb288b329cede649ae2`.
[CI run 34687706795](https://github.com/BitHighlander/keepkey-firmware/actions/runs/34687706795)
passed every required job on that exact head: formatting, static analysis,
secret and submodule checks, ARM builds (both products) with the runtime-SRAM
link gate, emulator builds, native unit suites, host integration for both
variants, the shared-library suite, generated-report validation and the CI
gate. `publish-emulator` is workflow_dispatch-only and is skipped, as designed.

Host results on that run: 1010 cases, 785 passed, 225 skipped, 0 failed, 389 OLED frames captured.
The evidence bundle (PDF, merged JUnit, manifest, checksum) is the
`test-report` artifact of that run and binds this firmware SHA and this
python-keepkey SHA.

This receipt is documentation only: the validated head named above is this
commit's parent, and the commit carrying these words changes no code, no test
and no pin. A reader checking the claim should diff this commit to see that.

Superseded: the head and run this section previously named predate the audit
below and no longer describe this tree.

## Full-diff audit, 2026-09-12

This candidate was audited by reviewing every changed file against its base,
not by re-reading the summaries above. 249 findings were raised across the
three release lines; the 104 rated P1/P2 were each re-checked by three
independent reviewers along separate lines of attack (is it reachable, does it
matter on a shipped release, is it already covered), and 84 survived that.
Everything that survived and applies to this line is fixed at the head below.

Each fix carries a test, and where a control was possible the unfixed code was
built and the test confirmed to fail against it -- a test that passes either
way is recorded as such in the file rather than counted as coverage.

Findings on this line are listed with their resolution in `audit-units/`.
The load-bearing ones for a reader of this receipt:

- cipher recovery committed the EMPTY mnemonic when a ceremony was driven with separators only -- shared with the other two lines and proven end to end against a control build
- a bitcoin-only-locked device refused settings writes only inside storage_commit(), which returns void: ChangePin, ChangeWipeCode, ApplySettings and ApplyPolicies took the user through their confirmations and reported success while nothing was written
- the THORChain deposit decoder was not pinned to a router, so deposit-shaped calldata to any contract inherited the clear-sign UX and skipped the AdvancedMode gate; the native amount screen showed the ABI word rather than msg.value
- the XRP ceiling was 1e6 too low and refused payments shipped firmware signed correctly; CACAO deposits rendered at zero decimals
- the commit CRC covered 642 of the 643 words of the V17 record, leaving the last byte of the encrypted secret outside write verification, while a unit test asserted the opposite
- 'Maximum priority fee' could be below what the Solana runtime charges, because the derived compute-unit limit gave the ComputeBudget instructions nothing (SIMD-0170 allocates them 3,000 each)
- auto-lock measured wall-clock rather than idleness and tore down live ceremonies
- this line was missing the 16 KiB runtime-SRAM link-time gate the other two carry

Two defects were found in the audit's own work and corrected the same way: a
router pin that refused Maya deposits (CI caught it), and a `const` violation
that only ARM GCC rejects (the emulator's clang accepts it). Local ARM
cross-builds using CI's own base image now cover every variant.
